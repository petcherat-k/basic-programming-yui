💻 ตัวอย่างโค้ดการเขียนโปรแกรมเบื้องต้น
🐍 Python: โค้ดที่เน้นความกระชับ
Python โดดเด่นด้วยไวยากรณ์ที่สะอาดตาและไม่ต้องประกาศชนิดข้อมูล

1. โครงสร้างพื้นฐาน
Python

# การแสดงผล
print("Hello from Python!")

# การกำหนดตัวแปร (Dynamically Typed)
num1 = 10
text1 = "Data Science"

# การดำเนินการทางคณิตศาสตร์
result = num1 * 5 + 2
print("Result:", result)
2. เงื่อนไขและการวนซ้ำ (Flow Control)
Python

score = 90

# เงื่อนไข (If/Else)
if score >= 80:
    print("Grade A")
elif score >= 70:
    print("Grade B")
else:
    print("Grade C")

# วนลูป For
for i in range(5):  # วนตั้งแต่ 0 ถึง 4
    print("Loop count:", i)
3. ฟังก์ชัน (Function)
Python

# การกำหนดฟังก์ชัน
def calculate_area(width, height):
    """คำนวณพื้นที่สี่เหลี่ยม"""
    return width * height

# การเรียกใช้ฟังก์ชัน
area = calculate_area(10, 5)
print("Area:", area)
📊 R: โค้ดที่เน้นการวิเคราะห์ข้อมูล
R ถูกออกแบบมาเพื่อการคำนวณทางสถิติและจัดการกับโครงสร้างข้อมูลเช่น Vector และ Data Frame

1. โครงสร้างพื้นฐานและการแสดงผล
R

# การแสดงผล
print("Hello from R!")

# การกำหนดตัวแปร (ใช้ <- แทน =)
my_data <- 15.5
my_text <- "Statistics"

# การดำเนินการพื้นฐาน
y <- my_data * 2
print(y)
2. โครงสร้างข้อมูล (Vector)
R

# การสร้าง Vector (ชุดข้อมูล)
ages <- c(22, 25, 20, 28, 30)

# การเรียกใช้ฟังก์ชันสถิติในตัว
average_age <- mean(ages)
standard_dev <- sd(ages)

print(paste("Average Age:", average_age))
3. เงื่อนไข
R

temp <- 32

if (temp > 30) {
    print("It's hot!")
} else if (temp > 20) {
    print("It's warm.")
} else {
    print("It's cool.")
}
☕ Java: โค้ดที่เน้นความเสถียรและ OOP
Java เป็นภาษาที่เน้นการเขียนเชิงวัตถุ (OOP) และต้องประกาศชนิดข้อมูลอย่างชัดเจน (Statically Typed)

1. โครงสร้างหลัก
Java

public class BasicJavaCode {
    // จุดเริ่มต้นของโปรแกรม
    public static void main(String[] args) {
        // 1. การแสดงผล
        System.out.println("Hello from Java!");

        // 2. การประกาศตัวแปร (ต้องกำหนดชนิดข้อมูล)
        int quantity = 5;
        double price = 10.99;
        String item = "Book";

        System.out.println("Item: " + item + ", Total Qty: " + quantity);
    }
}
2. การวนลูป (Looping)
Java

// ใน Main Method ของ Class
// วนลูป For
for (int i = 0; i < 3; i++) {
    System.out.println("Iteration: " + i);
}

// วนลูป While
int counter = 0;
while (counter < 2) {
    System.out.println("While count: " + counter);
    counter++;
}
3. ฟังก์ชัน (Method)
Java

// ต้องกำหนด Method ภายใน Class (นอก Main Method)
public static int addNumbers(int a, int b) {
    return a + b;
}

// การเรียกใช้ใน Main Method:
// int sum = addNumbers(15, 7);
// System.out.println("Sum is: " + sum);
